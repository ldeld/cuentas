<h2>Sesión iniciada como: <%= current_user.name %></h2>

<p>Mostrando gastos del mes de <%= Date.today.strftime("%B") %></p>

<p>Tus gastos: <%= current_user.total %></p>

<% User.where.not(id: current_user.id).each do |user| %>
  <p> Gastos de <%= user.name %>: <%= user.total %> </p>
<% end %>

<h3>Gastos totales de la casa: <%= @total_casa %></h3>
